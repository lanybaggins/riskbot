# Riskbot (Lany's fork)

This fork is deployed differently from upstream (`README` / `DEPLOYMENT.md`) — see `DEPLOYMENT_lany.md` for
the full setup and hosting steps, including the Docker-only, single-environment setup and the use of
`deploy-commands-lany.js` in place of `deploy-commands.js`.
